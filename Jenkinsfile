//pipeline {
   node("slavebits"){
   //with new branch staging
          stage("Build") {
              // Print a text to the console
              echo "BITS Jenkins Assignment 3"
          }
            stage("AWSCodeBuild") {
                // Just print a bits Pipeline to the console
                echo "Building the project in AWSCodeBuild"
            }
             stage("Deploy to Staging") {
             // Just print a bits Pipeline to the console
             echo "Deploying the project in staging"
            }
             stage("Deploy to Production") {
             // Just print a bits Pipeline to the console
             echo "Deploying the project in production"
            }
      // And next stages if you want to define further...
    }// End of slave slavebits

//} // End of pipeline
